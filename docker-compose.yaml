version: "2"
services:
  web:
    build: .
    ports:
    - "5000:5000"
    depends_on:
    - postgres
    volumes:
    - ./:/app
    command: sleep inf
  postgres:
    image: postgres
    ports:
    - "5432:5432"
    environment:
    - POSTGRES_USER=dev
    - POSTGRES_PASSWORD=mypassword